# CiteFinder runtime dependencies (install into a venv).
fastapi
uvicorn
pydantic
psycopg[binary]
pgvector
# Local embeddings via ONNX Runtime — deliberately NOT sentence-transformers /
# torch (hundreds of MB to multiple GB). See ADR 0007 and embedder.py.
onnxruntime
tokenizers
huggingface_hub
numpy
pypdf
openai
python-dotenv
